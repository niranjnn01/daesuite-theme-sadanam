<!-- Logo -->
                 
<div class="flex justify-center gap-4 items-center">
    <a href="/" class="flex items-center">
        <x-image src="{{tenant_asset('images/logo.png')}}" alt="Logo" class="h-12 w-auto" />
    </a>
    <span class="text-xl">{{ config('client.name') }}</span>
</div>