<!-- Logo -->
                 
<div class="flex justify-center gap-4 items-center">
    <a href="/" class="flex items-center">
        <x-image src="{{tenant_asset('images/logo.svg')}}" alt="Logo" class="h-30 w-auto" />
    </a>
    <span class="text-xl">{{ $organization->name }}</span>
</div>